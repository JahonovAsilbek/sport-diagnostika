.DS_Store
*.log
.vscode/
.idea/

# MS Office temp/lock files (e.g. ~$SPORT.docx)
~$*

# Machine-local Claude settings (per-machine, not synced)
.claude/settings.local.json

# Backup artifacts (scripts/backup.sh) — DB dumps + media tarballs, never committed
/backups/
